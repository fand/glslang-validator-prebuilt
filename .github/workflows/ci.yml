name: CI
on: push

jobs:
  build:
    runs-on:
      - ubuntu-latest
      - windows-latest
      - macos-latest
    steps:
      - uses: actions/checkout@v2
      - run: npm ci
      - run: npm test

